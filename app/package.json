{
  "name": "secure-artifact-signing-demo",
  "version": "1.0.0",
  "description": "Demo app for artifact signing with Cosign/Sigstore",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test":  "node -e \"require('./index.js')\" & sleep 2 && curl -sf http://localhost:3000/health && kill %1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
